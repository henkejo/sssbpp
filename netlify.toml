[build]
  command = "pnpm install && npx playwright install chromium && pnpm build"
  publish = ".next"

[build.environment]
  PLAYWRIGHT_BROWSERS_PATH = "0"

[[plugins]]
  package = "@netlify/plugin-nextjs"

